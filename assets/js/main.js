// Public site JS — minimal
document.addEventListener('DOMContentLoaded', function () {
  // Smooth nav active state already handled server-side.
  // Placeholder for future enhancements (sliders, filters).
});
